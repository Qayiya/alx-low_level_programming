#include "main.h"

/**
 * _isupper - checkes if a characer is uppercase
 * @c: variable text
 * Return:always zero
 */
int _ispeer(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
